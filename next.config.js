/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable Turbopack for now due to root detection issues
  // turbopack is not used with appDir by default in Next.js 16+
};

module.exports = nextConfig;
